# Normalize line endings across platforms.
* text=auto

# Shell scripts MUST be LF or they fail to run on macOS/Linux.
*.sh   text eol=lf
*.command text eol=lf

# Windows batch files stay CRLF.
*.bat  text eol=crlf
*.cmd  text eol=crlf
